import 'package:flutter/material.dart';

class changeTunes extends StatelessWidget {
  const changeTunes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
